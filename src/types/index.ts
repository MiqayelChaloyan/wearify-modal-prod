export type Image = {
    id: string;
    source: string;
};

export type PersonAttribute = {
    ID: string;
    Name: string;
    Age: string;
    Gender: string;
    Skin: string;
    source: string;
};

export type Product = {
    id: string;
    name: string;
    imgPath: string;
    closeUrl: string;
    isFemale: boolean;
};